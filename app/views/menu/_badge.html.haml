-link_prepend||=''
%ul.nav.nav-pills.nav-stacked
  - menu_hash.each do |menu_item|
    %li{:class => ('active' if params[:label]==menu_item[:label])}
      -#= link_to "/#{link}/#{menu_item[:name] : menu_item[:label]}",
      = link_to link + menu_item[label],
       :class => 'button white' do
        %span= menu_item[:label]
        %span.badge.pull-right= menu_item[:todo_count]