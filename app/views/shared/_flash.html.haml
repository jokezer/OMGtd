- flash.each do |key, value|
  - key = 'info' unless key==:success
  %div{class:"alert alert-#{key} alert-dismissable"}
    %button.close{type:'button', :'data-dismiss'=>'alert', :'aria-hidden'=>'true'} &times
    =value