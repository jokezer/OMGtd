.panel.panel-default
  .panel-body
    #{group_name.capitalize} todos:
    -if show_button
      =link_to link, class:'btn btn-xs pull-right' do
        All #{group_name}
        %span.glyphicon.glyphicon-forward
=render collection
