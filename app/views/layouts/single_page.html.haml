- content_for :application do
  :javascript
    $( document ).ready(function() {
      var todos = new Gtd.Collections.Todos();
      var p = todos.fetch();
      p.done(function () {
        window.router = new Gtd.Routers.Todos({todos: todos});
        Backbone.history.start();
      });
    });
= render template: 'layouts/application'